<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="phonebook">
	<!-- 목록 -->
	<select id="selectAll" resultType="PhonebookVo">
		SELECT id, name, hp, tel FROM
		PHONE_BOOK ORDER BY id
	</select>

	<!-- 게시물 입력 (INSERT) -->
	<insert id="insert" parameterType="PhonebookVo">
		INSERT INTO PHONE_BOOK(name,
		hp, tel)
		VALUES(#{name}, #{hp}, #{tel})
	</insert>

	<!-- 게시물 삭제 (DELETE) -->
	<delete id="delete" parameterType="PhonebookVo">
		DELETE FROM PHONE_BOOK
		WHERE id
		= #{id}
	</delete>

	<select id="search" resultType="PhonebookVo">
		SELECT id, name, hp, tel
		FROM PHONE_BOOK
		WHERE name LIKE '%' || #{name} || '%'
	</select>
</mapper>
